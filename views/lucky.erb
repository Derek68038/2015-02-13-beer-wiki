<p><em><h1>Lucky Bucket Brewing Company</h1></em></p>

<div class="picture">
  <img src="/lucky.jpeg" width="200" height="200" alt="Lucky Bucket"/>
</div>

<br>
<br>

<div>
  <h3>Description</h3>
  <em>Established in 2008. The name "Lucky Bucket" refers back to the days before kegs and bottles were            available, when the only way to get beer was to take a bucket to the local brewery, fill it up and lug       it back home. The name pays homage to our humble beginnings, the original Lucky Bucket.</em>
</div>

<br>
<br>

<div>
  <h3>Information</h3>
    <% @beer.find_by_name("Lucky Bucket Brewing Co") %>
    <% @beer.locations.each do |location| %>
      <em><p><strong><%= location.name %></strong></p>
      <p><%= location.street %></p> 
      <p><%= location.city %>,
      <%= location.state %>
      <%= location.zip %></p>
      <p><%= location.phone %></p>
      <p><a href="http://www.luckybucketbrewing.com/" title="To page">www.luckybucketbrewing.com</a></p></em>
   <% end %>
</div>

<br>

<div>
  <h3>Beer Reviews</h3>
    <% if @beer_brewery.length == 0 %>
      <em><%= "There have been no beers entered for this location." %></em>
    <% else %>
    <% @beer_brewery.each do |b| %>
      <% name = DATABASE.execute("SELECT style FROM styles WHERE id = #{b.style_id}") %>
      <% style_name = name[0].values[0]  %>
      <em><h4><%=b.beer%></h4>  
        <ul>
          <li><%= "Style - #{style_name}" %></li> 
          <li><%= "Color - #{b.color}" %></li> 
          <li><%= "IBUs - #{b.ibu}" %></li> 
          <li><%= "ABV - #{b.abv}%" %></li> 
          <li><%= "Review - #{b.review}" %></li>
          <li><%= "Date - #{b.date}" %></li></em>
        </ul>
        <br>
      <% end %>
    <% end %>
</div>  

<br>
  
<div class="enter">
  <p><a href="/home" title="To homepage">Click here to return to the main homepage</a></p>
</div>