<p><em><h1>Granite City Food and Brewery</h1></em></p>

<div class="picture">
  <img src="/granite.jpeg" width="150" height="150" alt="Granite City"/>
</div>

<br>
<br>

<div>
  <h3>Description</h3>
  <em>Established in 1999 and has locations across 14 states. All beers are brewed on-site. Despite having a       wide range of locations, their trademarked brewing process called Fermentus Interruptus allows them to       replicate the same great tastes throughout all their restaurants.</em>
</div>

<br>
<br>

<div>
  <h3>Information</h3>
    <% @beer.find_by_name("Granite City Food and Brewery") %>
    <% @beer.locations.each do |location| %>
      <% if location.state == "NE" %>
        <em><p><strong><%= location.name %></strong></p>
        <p><%= location.street %></p> 
        <p><%= location.city %>,
        <%= location.state %>
        <%= location.zip %></p>
        <p><%= location.phone %></p>
        <p><a href="http://www.gcfb.net/" title="To page">www.gcfb.net</a></p></em>
        <br>
      <% end %>
   <% end %>
</div>

<div>
  <h3>Beer Reviews</h3>
    <% if @beer_brewery.length == 0 %>
      <em><%= "There have been no beers entered for this location." %></em>
    <% else %>
    <% @beer_brewery.each do |b| %>
      <% name = DATABASE.execute("SELECT style FROM styles WHERE id = #{b.style_id}") %>
      <% style_name = name[0].values[0] %>
      <em><h4><%=b.beer%></h4>  
        <ul>
        <li><%= "Style - #{style_name}" %></li> 
        <li><%= "Color - #{b.color}" %></li> 
        <li><%= "IBUs - #{b.ibu}" %></li> 
        <li><%= "ABV - #{b.abv}%" %></li> 
        <li><%= "Review - #{b.review}" %></li>
        <li><%= "Date - #{b.date}" %></li></em>
        </ul>
        <br>
      <% end %>
    <% end %>
</div>

<br>

<div class="enter">
  <p><a href="/home" title="To homepage">Click here to return to the main homepage</a></p>
</div>